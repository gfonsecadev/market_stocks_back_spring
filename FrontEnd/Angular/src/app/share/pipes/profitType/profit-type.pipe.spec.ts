import { ProfitTypePipe } from './profit-type.pipe';

describe('ProfitTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ProfitTypePipe();
    expect(pipe).toBeTruthy();
  });
});
